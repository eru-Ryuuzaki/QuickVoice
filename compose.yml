services:
  quickvoice:
    build:
      context: .
      dockerfile: Dockerfile
      network: host
      args:
        HTTP_PROXY: http://127.0.0.1:7890
        HTTPS_PROXY: http://127.0.0.1:7890
        http_proxy: http://127.0.0.1:7890
        https_proxy: http://127.0.0.1:7890
    image: quickvoice:local
    container_name: quickvoice
    restart: unless-stopped
    ports:
      - "14003:4003"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 4003
      HOSTNAME: 0.0.0.0
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:4003/api/health"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 15s
